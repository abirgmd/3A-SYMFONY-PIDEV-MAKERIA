<form method="post" action="{{ path('app_order_informations_delete', {'id': order_information.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ order_information.id) }}">
    <button class="btn">Delete</button>
</form>
